User-agent: *
Allow: /
Disallow: /r/
Disallow: /stats

# Race pages (/r/{uuid}) are private per-pack URLs and should not be indexed.
# /stats is an internal operator dashboard.
# The landing page at / is the public, indexable entry point.

Sitemap: https://derby.987.ca/sitemap.xml
